name: kroften4/esp-lcd-ili9486-8bit-parallel
version: "0.0.1"
description: "ESP-IDF driver for ILI9486 LCD controller with 8bit parallel interface, forked from khiyamiftikhar/esp-lcd-ili9486"
url: "https://github.com/kroften4/esp-lcd-ili9486-8bit-parallel"
license: "MIT"

dependencies:
  idf: ">=6.0.0"

files:
   use_gitignore: true
   exclude:
      - ".git/**/*"
      - "docs/**/*"
      - "dist/**/*"
